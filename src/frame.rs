pub mod header;
pub mod parameters;

pub use header::{Header, LegacyHeader};

#[derive(Debug, Eq, PartialEq)]
pub enum Command {
    //todo!()
}

#[derive(Debug, Eq, PartialEq)]
pub enum Response {
    //todo!()
}

#[derive(Debug, Eq, PartialEq)]
pub enum Callback {
    //todo!()
}
